import { REALM } from '@/types/Realm';

const realm = {
  cdn: {
    domain: 'rvtrader',
    version: 'vLatest',
  },
  id: REALM.BOATMART,
  label: {
    plural: 'Boats',
    singular: 'Boat',
    singularWithArticle: 'a Boat',
  },
};

export { realm };
