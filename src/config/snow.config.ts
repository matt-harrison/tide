import { REALM } from '@/types/Realm';

const realm = {
  cdn: {
    domain: 'snowmobiletrader',
    version: 'vLatest',
  },
  id: REALM.SNOW,
  label: {
    plural: 'Snowmobiles',
    singular: 'Snowmobile',
    singularWithArticle: 'a Snowmobile',
  },
};

export { realm };
