import { REALM } from '@/types/Realm';

const realm = {
  cdn: {
    domain: 'commercialtrucktrader',
    version: 'vLatest',
  },
  id: REALM.TRUCK,
  label: {
    plural: 'Trucks',
    singular: 'Truck',
    singularWithArticle: 'a Truck',
  },
};

export { realm };
