import { REALM } from '@/types/Realm';

const realm = {
  cdn: {
    domain: 'cycletrader',
    version: 'vLatest',
  },
  id: REALM.CYCLE,
  label: {
    plural: 'Motorcycles',
    singular: 'Motorcycle',
    singularWithArticle: 'a Motorcycle',
  },
};

export { realm };
