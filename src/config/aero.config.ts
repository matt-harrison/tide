import { REALM } from '../types/Realm';

const realm = {
  cdn: {
    domain: 'aerotrader',
    version: 'vLatest',
  },
  id: REALM.AERO,
  label: {
    plural: 'Aircraft',
    singular: 'Aircraft',
    singularWithArticle: 'Aircraft',
  },
};

export { realm };
