import { REALM } from '../types/Realm';

const realm = {
  cdn: {
    domain: 'rvtrader',
    version: 'vLatest',
  },
  id: REALM.BOATLINE,
  label: {
    plural: 'Boats',
    singular: 'Boat',
    singularWithArticle: 'a Boat',
  },
};

export { realm };
