import { REALM } from '../types/Realm';

const realm = {
  cdn: {
    domain: 'equipmenttrader',
    version: 'vLatest',
  },
  id: REALM.EQUIP,
  label: {
    plural: 'Equipment',
    singular: 'Equipment',
    singularWithArticle: 'Equipment',
  },
};

export { realm };
